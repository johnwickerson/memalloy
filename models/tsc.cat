"BASIC"

include "basic.cat"
include "txn.cat"
let com = rf | fr | co
          
empty atom & (fre;coe) as AtomicRMW
      
let order = po | com

acyclic order as SC
acyclic stronglift(order, stxn) as TSC
